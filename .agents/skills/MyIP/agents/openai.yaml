interface:
  display_name: "MyIP"
  short_description: "Repo-specific patterns and workflows for MyIP"
  default_prompt: "Use the MyIP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true